import { ThemeDetectorDirective } from './theme-detector.directive';

describe('ThemeDetectorDirective', () => {
  it('should create an instance', () => {
    const directive = new ThemeDetectorDirective();
    expect(directive).toBeTruthy();
  });
});
